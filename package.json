{
  "name": "correo-particular",
  "version": "0.1.0",
  "private": true,
  "description": "Extensión para Gmail web que aplica reglas públicas en tu navegador: qué vence, qué es cada cosa y a quién debes respuesta. Nada sale del navegador.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check:no-network": "node scripts/check-no-network.mjs",
    "catalog:sync": "node scripts/sync-catalog.mjs",
    "check": "npm run typecheck && npm test && npm run check:no-network && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "@types/jsdom": "^30.0.0",
    "@types/node": "^24.13.6",
    "esbuild": "^0.28.2",
    "jsdom": "^30.1.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.9"
  }
}
